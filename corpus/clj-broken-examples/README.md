# clj-broken-examples

Intentionally broken Clojure snippets for agentic repair/evaluation.

- `syntax_error.clj.snippet`: malformed form sample (reader failure text fixture)
- `logic_error.clj`: compiles but behavior is wrong
